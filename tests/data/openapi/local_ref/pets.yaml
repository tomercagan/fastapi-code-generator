openapi: "3.0.0"
info:
  version: 1.0.0
  title: Swagger Petstore
  license:
    name: MIT
  description: |
    This description is for testing
    multi-line
    description

components:
  requestBodies:
    Pet:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/PetForm'
  parameters:
    MyParam:
      name: foo
      in: query
      schema:
        type: string
  responses:
    OK:
      description: OK
      content:
        application/json:
          schema:
            type: string
  schemas:
    PetForm:
      title: PetForm
      type: object
      properties:
        name:
          type: string
        age:
          type: integer